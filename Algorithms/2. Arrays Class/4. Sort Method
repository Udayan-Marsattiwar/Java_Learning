You are given an integer array arr[], you need to sort it in increasing order.

Examples:

Input: arr[] = [9, 8, 7, 6, 5, 4, 4]
Output: 4 4 5 6 7 8 9
Explanation: Just sorted array.
Input: arr[] = [99, 6, 33]
Output: 6 33 99
Explanation: Just sorted array.
Constraints:
1 <= arr.length <= 103
0 <= arri <= 106

class Solution {
    public static void sortArray(int arr[]) {
        // Your code here
        Arrays.sort(arr);

        // You do not need to print
    }
}
